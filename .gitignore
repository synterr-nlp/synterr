# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Testing/coverage
.coverage
.pytest_cache/
htmlcov/
coverage.xml

# IDE
.idea/
.vscode/
*.swp

# OS
.DS_Store

# Neural models (large, licensed externally)
models/

# LaTeX build artifacts
*.aux
*.log
*.nav
*.out
*.snm
*.toc
*.vrb

# Evaluation / scratch data
eval_punct.jsonl
lenta_sample.txt
lenta_sents.txt

# Scraped / intermediate files
rozental-contents.html
handoff.md

# Internal viewer (contains copyrighted Rozental text)
tools/diff_viewer_internal.html

# Review data (generated, not tracked)
tools/review/

# MkDocs build output
/site/

# Large external corpora (must never be tracked)
data/taiga/
data/wiki_200k.txt
data/rublimp_pool_sents.txt
data/mixed_sources_*.txt
data/scarce_sents_*.txt
data/conjunction_sents_*.txt
data/qwen_sft_*.jsonl
data/qwen_sft_*.dist.json

# Session tooling (Claude/agents) — but project hooks/permissions are committed
.claude/*
!.claude/settings.json
.agents/
.github/skills/

# Corpora and mined pools (regenerable via download / synterr mine-pools)
lenta_50k.txt
data/pools/
data/conjunction_sents.txt
data/conjunction_sents.meta.json
data/rare_conjunctions_*.txt
data/rublimp_pool_sents.meta.json
data/french_poc/

# Unified-dict build intermediates (regenerable via scripts/build_unified_dict.py)
scripts/dict.opcorpora.xml
scripts/dict.opcorpora.xml.bz2
scripts/need_stress.txt
scripts/need_morphemes.txt
scripts/stress_results.json
scripts/morpheme_results.json
scripts/zalizniak_stress.json

# Presentation lane artifacts (kept out of the code repo)
docs/presentation.tex
docs/presentation.pdf
docs/synterr_presentation.pptx
docs/references.bib
